{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "API REST para el proyecto CRUD",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 0" 
    // ^ Si añades pruebas, reemplaza este comando.
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5" 
    // ^ Añade aquí las dependencias que uses (ej. pg para PostgreSQL)
  }
}
